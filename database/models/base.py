from sqlalchemy.orm import declarative_base
from core.config import settings  # Ensure this file exists and is correctly configured file exists and is correctly configured

Base = declarative_base()